export const LIMITS = Object.freeze({
  usernameChars: 39,
  requestBodyBytes: 2 * 1024,
  maxCandidates: 30,
  maxRepositories: 20,
  searchPages: 1,
  searchBytes: 2 * 1024 * 1024,
  searchMs: 10_000,
  diffBytes: 5 * 1024 * 1024,
  diffMs: 15_000,
  maxFilesPerPr: 300,
  maxPathsPerPr: 500,
  maxRawAdditionsPerPr: 20_000,
  maxEligibleAdditionsPerPr: 10_000,
  maxEligibleAdditions: 100_000,
  restCalls: 100,
  graphqlCalls: 60,
  githubCalls: 140,
  restBytes: 100 * 1024 * 1024,
  graphqlBytes: 32 * 1024 * 1024,
  commitShasPerPr: 250,
  treeEntriesPerRepository: 100_000,
  treeEntries: 300_000,
  blobBytes: 1024 * 1024,
  blobBytesPerRepository: 64 * 1024 * 1024,
  blobBytesTotal: 128 * 1024 * 1024,
  blobsPerRepository: 20_000,
  blamePathsPerPr: 300,
  blameRangesPerPr: 10_000,
  blameRanges: 50_000,
  gitProcesses: 60,
  gitCommandMs: 30_000,
  gitTotalMs: 180_000,
  gitStdoutBytes: 16 * 1024 * 1024,
  gitStderrBytes: 1024 * 1024,
  workspaceThresholdBytes: 480 * 1024 * 1024,
  runMs: 600_000,
  queueMs: 120_000,
  statusTtlMs: 60 * 60_000,
  resultTtlMs: 30 * 60_000,
  failureTtlMs: 15 * 60_000,
  cacheEntries: 100,
  cacheBytes: 64 * 1024 * 1024,
  resultBytes: 1024 * 1024,
  globalRatePerMinute: 10,
  globalBurst: 4,
  clientRatePerMinute: 2,
  clientBurst: 2,
  pollRatePerMinute: 30,
} as const);

export type EffectiveLimits = typeof LIMITS;
export const ALGORITHM_VERSION = "line-v1-path-v2";
export const SELECTION_VERSION = "github-search-v1";
